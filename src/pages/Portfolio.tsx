import * as React from 'react';
import { BioDetails } from '../components/BioDetails';

export const PortfolioPage = () => {
    return(
        <div>
            <BioDetails />
        </div>
    );
}